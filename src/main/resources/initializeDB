CREATE DATABASE fair_beans_database
    WITH
    OWNER = postgres
    ENCODING = 'UTF8'
    CONNECTION LIMIT = -1;

CREATE TABLE partners(
	id serial primary key,
	name character varying(200) NOT NULL,
	street character varying(200),
	house_number character varying(200),
	zip_code character varying(200),
	city character varying(200),
	link character varying(200)
);

INSERT INTO partners (name, street, house_number, zip_code, city, link)
VALUES
	('Dream Café', 'Highroad', '27', '3214', 'Newtown', 'www.dream_cafecom'),
	('Cozy Rozie', 'Mixlane', '2', '6548', 'Maicity', 'www.cozy_rozie.com'),
	('The Coffie Gang', 'Firestreet', '66', '5332', 'Lipcity', 'www.the_coffie_gang.com'),
	('Beans and Milk', 'Alabastarroad', '2', '6548', 'Moldue', 'www.beans_and_milk.com'),
	('Creamy', 'Redlane', '66', '5332', 'Pelikan', 'www.creamy.com');

CREATE TABLE products(
	id serial primary key,
	name character varying(200) NOT NULL,
	description character varying(600)
);

INSERT INTO products (name, description)
VALUES
	('colombian blond roast', 'Velvety coffee with a note of caramel'),
	('diamond blend', 'Keeps you awake through the longest days');

